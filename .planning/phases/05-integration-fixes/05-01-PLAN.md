---
phase: 05-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/index.ts
  - src/storage/storage-service.ts
  - src/storage/storage-service.test.ts
autonomous: true
requirements:
  - DATA-04
gap_closure: true

must_haves:
  truths:
    - "DOOR_HARDWARE_DEFAULTS is importable from @/data barrel export"
    - "Migrated v2 line items have doorHardwareCost initialized to 0"
    - "All existing 157+ tests continue to pass with new assertions added"
  artifacts:
    - path: "src/data/index.ts"
      provides: "Barrel re-export of DOOR_HARDWARE_DEFAULTS"
      contains: "DOOR_HARDWARE_DEFAULTS"
    - path: "src/storage/storage-service.ts"
      provides: "doorHardwareCost field in v2->v3 migration"
      contains: "doorHardwareCost"
    - path: "src/storage/storage-service.test.ts"
      provides: "Migration test assertions for doorHardwareCost"
      contains: "doorHardwareCost"
  key_links:
    - from: "src/data/index.ts"
      to: "src/data/seed-door-hardware.ts"
      via: "named import and re-export"
      pattern: "import.*DOOR_HARDWARE_DEFAULTS.*from.*seed-door-hardware"
    - from: "src/storage/storage-service.ts"
      to: "src/types/index.ts"
      via: "LineItem.doorHardwareCost field initialization in migration"
      pattern: "doorHardwareCost.*\\?\\?.*0"
---

<objective>
Close two integration gaps identified by the v1.0 milestone audit so that Phases 3 (State & Behavior) and 4 (UI Components) can proceed without blockers.

Purpose: The barrel export gap blocks Phase 3's auto-populate hook from importing DOOR_HARDWARE_DEFAULTS via @/data. The migration gap leaves doorHardwareCost as undefined on migrated line items, violating the LineItem interface type contract and risking NaN display bugs.

Output: Three files patched (barrel export, migration, migration tests), full test suite green.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-fixes/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/data/seed-door-hardware.ts (lines 26-58):
```typescript
export const DOOR_HARDWARE_DEFAULTS: Record<string, DoorHardwareEntry[]> = {
  'sys-006': [ ... ],  // Entrance System
  'sys-007': [ ... ],  // Revolving Door
  'sys-008': [ ... ],  // Sliding Door
  'sys-009': [ ... ],  // Swing Door
}
```

From src/data/index.ts (line 7 -- current import, missing DOOR_HARDWARE_DEFAULTS):
```typescript
import { SEED_DOOR_HARDWARE } from './seed-door-hardware'
```

From src/storage/storage-service.ts (lines 46-60 -- v2->v3 migration block):
```typescript
if (version < 3) {
  settings = {
    ...settings,
    doorHardware: defaults.settings.doorHardware,
  }
  projects = projects.map(p => ({
    ...p,
    lineItems: (p.lineItems ?? []).map((li: any) => ({
      ...li,
      doorHardware: li.doorHardware ?? [],
      // MISSING: doorHardwareCost: li.doorHardwareCost ?? 0,
    })),
  }))
  version = 3
}
```

From src/types/index.ts (LineItem interface -- doorHardwareCost is `number`, not optional):
```typescript
doorHardwareCost: number
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DOOR_HARDWARE_DEFAULTS barrel export and fix v2-v3 migration</name>
  <files>src/data/index.ts, src/storage/storage-service.ts</files>
  <action>
    Two one-line fixes in existing files:

    1. **Barrel export** (`src/data/index.ts`):
       - Update line 7 import to include DOOR_HARDWARE_DEFAULTS:
         `import { SEED_DOOR_HARDWARE, DOOR_HARDWARE_DEFAULTS } from './seed-door-hardware'`
       - Add a named re-export after the `createDefaultAppState` function:
         `export { DOOR_HARDWARE_DEFAULTS }`
       - Do NOT re-export SEED_DOOR_HARDWARE (it is consumed internally by DEFAULT_SETTINGS only).

    2. **Migration fix** (`src/storage/storage-service.ts`):
       - In the v2->v3 migration block (the `if (version < 3)` section), inside the lineItems map, add one line after `doorHardware: li.doorHardware ?? [],`:
         `doorHardwareCost: li.doorHardwareCost ?? 0,`
       - Use `??` (nullish coalescing), NOT `||` -- matches existing project convention on the adjacent doorHardware line.
       - Do NOT modify any other migration block or restructure migration logic.
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run src/storage/storage-service.test.ts</automated>
  </verify>
  <done>
    - `import { DOOR_HARDWARE_DEFAULTS } from '@/data'` resolves without TypeScript errors
    - v2->v3 migration line items include `doorHardwareCost: 0` for items that lacked the field
    - Existing storage tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add doorHardwareCost assertions to migration tests</name>
  <files>src/storage/storage-service.test.ts</files>
  <behavior>
    - v2->v3 migration test: migrated line item has doorHardwareCost === 0
    - v1->v2->v3 sequential migration test: migrated line item has doorHardwareCost === 0
    - v1->v3 full migration test: migrated line item has doorHardwareCost === 0
  </behavior>
  <action>
    Add `expect(li.doorHardwareCost).toBe(0)` assertions to three existing migration tests in `src/storage/storage-service.test.ts`:

    1. **"migrates v2 data to v3" test** (around line 124-125, after `expect(li.doorHardware).toEqual([])`):
       Add: `expect(li.doorHardwareCost).toBe(0)`

    2. **"migrates v1 data through v2 and v3 in sequence" test** (around line 159, after `expect(li.doorHardware).toEqual([])`):
       Add: `expect(li.doorHardwareCost).toBe(0)`

    3. **"migrates old schema version to current" test** (around line 70, after `expect(li.doorHardware).toEqual([])`):
       Add: `expect(li.doorHardwareCost).toBe(0)`

    These assertions verify the Task 1 migration fix. They must FAIL without the migration fix (doorHardwareCost would be undefined) and PASS with it.

    Do NOT add new test cases -- add assertions to existing tests only.
    Do NOT modify any existing assertions or test data.
  </action>
  <verify>
    <automated>npx vitest run src/storage/storage-service.test.ts</automated>
  </verify>
  <done>
    - Three migration tests each assert doorHardwareCost === 0 on migrated line items
    - All storage-service tests pass (existing + new assertions)
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification:

1. `npx tsc -b` -- TypeScript compiles with no errors (confirms barrel export and type correctness)
2. `npx vitest run` -- Full test suite passes (all 157+ existing tests plus new assertions)
3. `npx vitest run src/storage/storage-service.test.ts` -- Migration tests specifically pass with doorHardwareCost assertions
4. Verify DOOR_HARDWARE_DEFAULTS is importable: `npx tsc -b` success with the barrel export confirms this (TypeScript would error if the re-export were missing or mistyped)
</verification>

<success_criteria>
- DOOR_HARDWARE_DEFAULTS importable from @/data (unblocks Phase 3 UI-01 and Phase 4 UI-05)
- v2->v3 migration initializes doorHardwareCost: 0 on migrated line items (DATA-04 completeness)
- Three migration tests assert doorHardwareCost === 0 (regression protection)
- Full test suite green: `npx vitest run` passes all tests
- TypeScript clean: `npx tsc -b` produces zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-fixes/05-01-SUMMARY.md`
</output>
