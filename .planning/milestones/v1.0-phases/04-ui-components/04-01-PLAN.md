---
phase: 04-ui-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/TakeoffView.tsx
autonomous: true
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "Compact sub-row with hardware chips and subtotal appears below each door line item"
    - "Sub-row does NOT appear for non-door line items"
    - "Door hardware cost is shown as a separate indented sub-line under Material in the expanded cost breakdown"
    - "Estimator can check/uncheck hardware items and change quantities in the expanded panel"
    - "Reset to Defaults button restores the default hardware set for the current door type"
  artifacts:
    - path: "src/views/TakeoffView.tsx"
      provides: "DoorHardwareSubRow component, DoorHardwarePanel component, modified LineItemRow with hook wiring and conditional door/non-door rendering"
      contains: "DoorHardwareSubRow"
  key_links:
    - from: "src/views/TakeoffView.tsx (LineItemRow)"
      to: "src/hooks/use-door-hardware.ts"
      via: "useDoorHardware(projectId, item.id) call inside LineItemRow"
      pattern: "useDoorHardware\\(projectId"
    - from: "src/views/TakeoffView.tsx (DoorHardwareSubRow)"
      to: "item.doorHardware and item.doorHardwareCost"
      via: "reads pre-computed fields from LineItem prop"
      pattern: "item\\.doorHardware\\.map"
    - from: "src/views/TakeoffView.tsx (DoorHardwarePanel)"
      to: "useDoorHardware hook functions"
      via: "addDoorHardware, removeDoorHardware, updateDoorHardwareQty callbacks"
      pattern: "addDoorHardware|removeDoorHardware|updateDoorHardwareQty"
    - from: "src/views/TakeoffView.tsx (Reset button)"
      to: "onUpdate(item.id, { doorHardware: getDefaultDoorHardware(...) })"
      via: "existing updateLineItem path which recalculates and cascades"
      pattern: "getDefaultDoorHardware"
---

<objective>
Add door hardware UI to TakeoffView: a compact sub-row showing hardware chips below door line items, a door hardware editing panel with checkboxes and quantity inputs in the expanded detail section, door hardware cost sub-lines in the cost breakdown, and a Reset to Defaults button.

Purpose: This is the final phase of the Door Hardware Selection milestone. All data model, calculation, and state/behavior layers are complete. This plan wires the existing hooks and utilities into visible UI so estimators can see, edit, and reset door hardware selections directly in the Takeoff view.

Output: Modified TakeoffView.tsx with DoorHardwareSubRow and DoorHardwarePanel local components, conditional rendering for door vs non-door line items, and a fully interactive hardware editing workflow.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-components/04-CONTEXT.md
@.planning/phases/04-ui-components/04-RESEARCH.md
@.planning/phases/03-state-behavior/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/types/index.ts:
```typescript
export interface DoorHardwareEntry {
  hardwareId: string   // References Hardware.id in settings.doorHardware
  quantity: number     // Per-door quantity (e.g., hinges=3)
}

// On LineItem:
//   doorHardwareCost: number  // Derived: door hardware cost included in materialCost
//   doorHardware: DoorHardwareEntry[]  // Door-specific hardware with per-item quantities

// On AppSettings:
//   doorHardware: Hardware[]   // Door hardware catalog (12 items, dhw-xxx IDs)
```

From src/calc/index.ts (barrel exports):
```typescript
export { isDoorSystemType, DOOR_SYSTEM_IDS } from './door-system-util'
export { getDefaultDoorHardware, applyDoorHardwareAutoPopulate } from './door-hardware-helpers'
export { calcDoorHardwareCost } from './door-hardware-calc'
// Also: formatCurrency, calcBenchmark, shouldSuggestEquipment, calcRunningTotals
```

From src/hooks/use-door-hardware.ts:
```typescript
export function useDoorHardware(projectId: string, lineItemId: string): {
  doorHardware: DoorHardwareEntry[]
  addDoorHardware: (hardwareId: string, quantity?: number) => void
  removeDoorHardware: (hardwareId: string) => void
  updateDoorHardwareQty: (hardwareId: string, quantity: number) => void
}
```

From src/calc/door-hardware-helpers.ts:
```typescript
export function getDefaultDoorHardware(
  systemTypeId: string,
  heightInches: number,
): DoorHardwareEntry[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook wiring, DoorHardwareSubRow, and cost breakdown sub-lines</name>
  <files>src/views/TakeoffView.tsx</files>
  <action>
Modify TakeoffView.tsx to wire the door hardware hook and add the sub-row and cost breakdown changes:

1. **Add imports:** Import `useDoorHardware` from `@/hooks/use-door-hardware`, `isDoorSystemType` and `getDefaultDoorHardware` from `@/calc`, and `DoorHardwareEntry` type from `@/types`.

2. **Add `projectId` prop to LineItemRowProps:** Add `projectId: string` to the `LineItemRowProps` interface. Update the destructuring in the `LineItemRow` memo function to include `projectId`.

3. **Pass `projectId` from TakeoffView:** In the `TakeoffView` component where `<LineItemRow>` is rendered inside the `.map()`, add `projectId={id!}` prop.

4. **Wire useDoorHardware inside LineItemRow:** At the top of the `LineItemRow` function (BEFORE any conditional returns), call `const { addDoorHardware, removeDoorHardware, updateDoorHardwareQty } = useDoorHardware(projectId, item.id)`. The hook MUST be called unconditionally per React rules -- it gracefully handles non-door items.

5. **Compute `isDoor` flag:** Add `const isDoor = isDoorSystemType(item.systemTypeId)` inside `LineItemRow` after the existing `systemType` lookup.

6. **Create `DoorHardwareSubRow` component** as a local function component OUTSIDE the `memo` call (place it between the `LineItemRow` memo and the `TotalRow` helper at the bottom of the file). Per user decision: inline chips format, always visible for door items with hardware, no toggle/collapse needed.

    Props: `{ item: LineItem; settings: AppSettings }`

    Render:
    - Outer div: `className="flex flex-wrap items-center gap-1.5 border-t border-gray-100 px-3 py-1.5 pl-8 text-xs text-gray-500"`
    - Map over `item.doorHardware`, for each entry look up `settings.doorHardware.find(h => h.id === entry.hardwareId)`. If not found, return null. Render a chip span: `className="inline-flex items-center rounded-full bg-gray-100 px-2 py-0.5 text-gray-600"`. Content: `{hw.name}` followed by `\u00D7${entry.quantity}` only when `entry.quantity > 1` (use the multiplication sign unicode character).
    - After the chips, render the hardware subtotal: `<span className="ml-auto font-medium text-gray-700">{formatCurrency(item.doorHardwareCost)}</span>`

7. **Insert DoorHardwareSubRow in LineItemRow render:** Between the validation errors section and the expanded panel section (the `{isExpanded && ...}` block), add:
    ```
    {isDoor && item.doorHardware.length > 0 && (
      <DoorHardwareSubRow item={item} settings={settings} />
    )}
    ```
    This renders the sub-row only for door items with at least one hardware entry. Per research open question #1: show only when doorHardware.length > 0 to avoid an empty row.

8. **Modify cost breakdown for door items (UI-04):** In the expanded panel's "Cost Breakdown" section, replace the existing Material line with conditional rendering:
    - For door items (`isDoor`): Show the Material total line, then two indented sub-lines:
      - "Glass + Frame" with value `formatCurrency(item.materialCost - item.doorHardwareCost)` using `className="flex justify-between pl-3"` with `text-gray-400` for both label and value
      - "Door Hardware" with value `formatCurrency(item.doorHardwareCost)` using same indented style
    - For non-door items: Keep the existing simple Material line unchanged
    - The Labor, Equipment, Total, and other breakdown lines remain unchanged for both cases.

IMPORTANT anti-patterns to avoid:
- Do NOT wrap `useDoorHardware` in an `if (isDoor)` check -- hooks must be called unconditionally.
- Do NOT compute door hardware cost manually -- use `item.doorHardwareCost` (pre-computed by calcFullLineItem).
- Do NOT re-run `calcMaterialCost` for glass+frame -- compute as `item.materialCost - item.doorHardwareCost`.
- Use optional chaining for `settings.doorHardware.find()` result (TypeScript strict mode with noUncheckedIndexedAccess).
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearEstimate && npm run lint && npm test</automated>
  </verify>
  <done>
    - DoorHardwareSubRow renders chips for each door hardware entry with name and quantity
    - Sub-row shows hardware subtotal at right end
    - Sub-row appears only for door line items with doorHardware.length > 0
    - Sub-row does NOT appear for non-door line items
    - Cost breakdown shows "Glass + Frame" and "Door Hardware" indented sub-lines for door items
    - Cost breakdown shows simple "Material" line for non-door items
    - TypeScript compiles cleanly, all existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: DoorHardwarePanel with editing checkboxes, quantity inputs, and Reset to Defaults</name>
  <files>src/views/TakeoffView.tsx</files>
  <action>
Continue modifying TakeoffView.tsx to add the door hardware editing panel in the expanded section:

1. **Create `DoorHardwarePanel` component** as a local function component OUTSIDE the `memo` call (place it near `DoorHardwareSubRow`). Per user decision: checkbox + quantity input pattern, all 12 door hardware items listed, checked = in selection, unchecked = available to add.

    Props:
    ```typescript
    {
      item: LineItem
      settings: AppSettings
      onAdd: (hardwareId: string) => void
      onRemove: (hardwareId: string) => void
      onUpdateQty: (hardwareId: string, quantity: number) => void
      onReset: () => void
    }
    ```

    Render:
    - Section label: `<label className="text-xs font-medium text-gray-600">Door Hardware</label>`
    - Container: `<div className="mt-1 space-y-1">`
    - Map over `settings.doorHardware` (all 12 catalog items). For each `hw`:
      - Look up `const entry = item.doorHardware.find(e => e.hardwareId === hw.id)`
      - `const isSelected = !!entry`
      - Render a label: `className="flex items-center gap-2 text-xs"`
      - Checkbox: `type="checkbox"`, `checked={isSelected}`, `onChange` calls `onRemove(hw.id)` if selected or `onAdd(hw.id)` if not. Class: `"h-3.5 w-3.5 rounded border-gray-300 text-blue-600"` (matches existing hardware/conditions checkboxes).
      - Hardware name span: `className={isSelected ? '' : 'text-gray-400'}` with `{hw.name}`
      - If selected AND entry exists: render quantity input `type="number"`, `value={entry.quantity}`, `min={1}`, onChange calls `onUpdateQty(hw.id, Math.max(1, parseInt(e.target.value) || 1))`. Class: `"w-12 rounded border border-gray-200 px-1 py-0.5 text-center text-xs"`. Per research pitfall #2: clamp min=1 to prevent accidental removal.
      - If NOT selected: render unit cost span `className="text-gray-400"` with `{formatCurrency(hw.unitCost)}/ea`
    - After the checkbox list, render Reset to Defaults button:
      - `<button type="button" onClick={onReset} className="mt-2 rounded border border-gray-300 px-2 py-1 text-xs text-gray-600 hover:bg-gray-100">Reset to Defaults</button>`
      - Per user decision: no confirmation dialog, button always enabled, immediate reset.

2. **Wire DoorHardwarePanel in expanded panel:** In the expanded panel's 2-column grid, find the existing Hardware section (the `<div>` containing `<label>Hardware</label>` and `settings.hardware.map(...)`). Replace it with conditional rendering:
    - For door items (`isDoor`): render `<DoorHardwarePanel>` with props:
      - `item={item}`
      - `settings={settings}`
      - `onAdd={addDoorHardware}` (from useDoorHardware hook, already wired in Task 1)
      - `onRemove={removeDoorHardware}` (from useDoorHardware hook)
      - `onUpdateQty={updateDoorHardwareQty}` (from useDoorHardware hook)
      - `onReset` as an inline handler: `() => onUpdate(item.id, { doorHardware: getDefaultDoorHardware(item.systemTypeId, item.heightInches) })`. This uses the existing `onUpdate` (updateLineItem) path which already handles recalc and VE cascade. Per research Pattern 3: zero new hook code needed.
    - For non-door items: keep the existing generic hardware checkboxes exactly as they are.

IMPORTANT anti-patterns to avoid:
- Do NOT define DoorHardwarePanel inside the memo boundary -- define it as a standalone function component.
- Do NOT create inline arrow functions for onAdd/onRemove/onUpdateQty when passing to DoorHardwarePanel -- the hook's returned functions are already stable useCallback references.
- The `onReset` handler IS an inline arrow function calling `onUpdate`, which is acceptable since DoorHardwarePanel is not itself memo'd. If memoization is needed later, this can be wrapped in useCallback.
- Use `Math.max(1, parseInt(e.target.value) || 1)` for quantity input to prevent zero/negative/NaN values.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearEstimate && npm run lint && npm test</automated>
  </verify>
  <done>
    - DoorHardwarePanel shows all 12 door hardware catalog items with checkboxes
    - Checked items show quantity input; unchecked items show unit cost
    - Checking a new item calls addDoorHardware (default qty 1)
    - Unchecking an item calls removeDoorHardware
    - Changing quantity calls updateDoorHardwareQty with min=1 clamping
    - Reset to Defaults button calls onUpdate with getDefaultDoorHardware result
    - Door items show DoorHardwarePanel; non-door items show existing generic hardware checkboxes
    - TypeScript compiles cleanly, all existing tests pass
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full phase:

1. **TypeScript compilation:** `npm run lint` passes with zero errors
2. **All tests pass:** `npm test` runs all existing tests plus any new tests, all green
3. **Calc verification:** `npm run verify` passes all 46 assertions (37 original + 9 door hardware)
4. **Visual check (manual):** Run `npm run dev`, navigate to a project's Takeoff view:
   - Create or open a line item with a door system type (Swing Door, Sliding Door, Revolving Door, or Entrance System)
   - Verify sub-row with hardware chips appears below the collapsed row
   - Expand the line item -- verify door hardware editing panel with checkboxes and quantities
   - Verify cost breakdown shows "Glass + Frame" and "Door Hardware" sub-lines under Material
   - Click "Reset to Defaults" -- verify hardware list resets to the door type's defaults
   - Add a non-door line item (e.g., Curtain Wall) -- verify NO sub-row, generic hardware checkboxes in expanded panel, simple Material line in cost breakdown
</verification>

<success_criteria>
- Sub-row with hardware chips visible below every door line item that has hardware entries
- Sub-row absent for non-door line items (Curtain Wall, Window Wall, Storefront, etc.)
- Door hardware editing panel replaces generic hardware section for door items only
- All 12 door hardware catalog items shown as checkboxes with quantity inputs for selected items
- Reset to Defaults button works and restores door type defaults
- Cost breakdown shows indented sub-lines (Glass + Frame, Door Hardware) for door items
- No TypeScript errors, all tests pass, verify-calc passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-components/04-01-SUMMARY.md`
</output>
