{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -Command \"gitleaks protect --staged --no-banner; if($LASTEXITCODE -ne 0){Write-Error 'Secrets detected! Remove them before committing.'; exit 2}\""
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Read",
      "Grep",
      "Glob",
      "LS",
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git log*)"
    ],
    "deny": []
  }
}